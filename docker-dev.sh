#!/bin/bash 

# rm package-lock.json
# npm install serverless-webpack
# sls dynamodb start --migrate --dbPath=/var/task/dynamodb_data && serverless offline --skipCacheInvalidation --host 0.0.0.0 start -r ap-southeast-1 --noTimeout
# sls dynamodb install
# sls dynamodb start --migrate --dbPath=/var/task/dynamodb_data && /
# sls offline --host 0.0.0.0 start --noTimeout
# npm link webpack
# npm install serverless-offline --save-dev

# tail -f /dev/null

